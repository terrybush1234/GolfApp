<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The theme applied to the application or activity before the splash screen is shown. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and destination theme. -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Set the theme of the Activity that follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.GolfApp</item>
    </style>

    <!-- Base application theme applied after the splash screen. -->
    <style name="Theme.GolfApp" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
